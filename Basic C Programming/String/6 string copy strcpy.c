#include<stdio.h>
#include<cstring>

int main()
{
	// freopen("input.txt","r",stdin);
	// freopen("output.txt","w",stdout);

	char st[20];
	char tr[20];

	gets(st);

	strcpy(tr,st);
	printf("%s",tr);

}